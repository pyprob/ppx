-- automatically generated by the FlatBuffers compiler, do not modify

-- namespace: ppx

local flatbuffers = require('flatbuffers')

local Run = {} -- the module
local Run_mt = {} -- the class metatable

function Run.New()
    local o = {}
    setmetatable(o, {__index = Run_mt})
    return o
end
function Run.GetRootAsRun(buf, offset)
    if type(buf) == "string" then
        buf = flatbuffers.binaryArray.New(buf)
    end
    local n = flatbuffers.N.UOffsetT:Unpack(buf, offset)
    local o = Run.New()
    o:Init(buf, n + offset)
    return o
end
function Run_mt:Init(buf, pos)
    self.view = flatbuffers.view.New(buf, pos)
end
function Run.Start(builder) builder:StartObject(0) end
function Run.End(builder) return builder:EndObject() end

return Run -- return the module